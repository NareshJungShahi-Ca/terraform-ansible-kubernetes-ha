
- name: "Install containerd"
  ansible.builtin.apt:
     name: containerd
     state: present
     update_cache: yes

- name: Enable and start containerd
  ansible.builtin.systemd:
    name: containerd
    enabled: yes
    state: started
